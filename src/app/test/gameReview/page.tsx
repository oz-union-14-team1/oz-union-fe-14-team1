// 같은 폴더에 있으므로 ./ 를 써서 가져옵니다.

import { GameReview } from '@/components/layout/review/GameReview'

export default function GameReviewPage() {
  return (
    <div>
      <GameReview />
    </div>
  )
}
